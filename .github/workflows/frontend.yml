name: Frontend CI/CD

on:
  push:
    branches:
      - main
      - master
    paths:
      - "frontend/**"
      - ".github/workflows/frontend.yml"
  workflow_dispatch:

env:
  REGISTRY: docker.io
  IMAGE_NAME: ${{ secrets.DOCKER_USERNAME }}/bkeep-frontend

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          dockerfile: ./frontend/Dockerfile
          target: production
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VITE_API_ENDPOINT=${{ secrets.VITE_API_ENDPOINT || 'http://72.62.161.70:8000/api/v1' }}
          cache-from: type=registry,ref=${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.IMAGE_NAME }}:buildcache,mode=max

      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd ${{ secrets.DEPLOY_PATH }}

            # Pull latest image
            echo "Pulling latest frontend image..."
            docker pull ${{ env.IMAGE_NAME }}:latest

            # Update docker-compose to use Docker Hub image instead of build
            # Create a temporary override file
            cat > docker-compose.override.yml << 'EOF'
            services:
              frontend:
                image: ${IMAGE_NAME}:latest
            EOF
            # Replace placeholder with actual image name
            sed -i "s|\${IMAGE_NAME}|${{ env.IMAGE_NAME }}|g" docker-compose.override.yml

            # Use docker-compose for zero-downtime deployment
            # This will recreate the container with the new image
            echo "Updating frontend container..."
            docker compose -f docker-compose.production.yml -f docker-compose.override.yml pull frontend
            docker compose -f docker-compose.production.yml -f docker-compose.override.yml up -d --no-deps --force-recreate frontend

            # Wait for health check
            echo "Waiting for frontend to be healthy..."
            MAX_WAIT=60
            WAIT_TIME=0
            while [ $WAIT_TIME -lt $MAX_WAIT ]; do
              if docker compose -f docker-compose.production.yml -f docker-compose.override.yml exec -T frontend wget --quiet --tries=1 --spider --timeout=5 http://localhost:80 2>/dev/null; then
                echo "✅ Frontend is healthy!"
                break
              fi
              echo "Waiting for health check... ($WAIT_TIME/$MAX_WAIT seconds)"
              sleep 5
              WAIT_TIME=$((WAIT_TIME + 5))
            done

            # Check if container is running and healthy
            if docker ps | grep -q "bkeep-frontend-prod"; then
              # Verify health endpoint one more time
              if docker compose -f docker-compose.production.yml -f docker-compose.override.yml exec -T frontend wget --quiet --tries=1 --spider --timeout=5 http://localhost:80 2>/dev/null; then
                echo "✅ Frontend deployed successfully and healthy!"

                # Clean up old images (keep last 2)
                docker image prune -f --filter "until=24h"

                # Remove temporary override file
                rm -f docker-compose.override.yml
              else
                echo "⚠️ Container running but health check failed after $MAX_WAIT seconds"
                docker compose -f docker-compose.production.yml -f docker-compose.override.yml logs --tail=50 frontend
                rm -f docker-compose.override.yml
                exit 1
              fi
            else
              echo "❌ Deployment failed - container not running"
              docker compose -f docker-compose.production.yml -f docker-compose.override.yml ps frontend
              rm -f docker-compose.override.yml
              exit 1
            fi

      - name: Verify deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd ${{ secrets.DEPLOY_PATH }}
            COMPOSE_FILES="-f docker-compose.production.yml"
            if [ -f docker-compose.override.yml ]; then
              COMPOSE_FILES="$COMPOSE_FILES -f docker-compose.override.yml"
            fi
            echo "=== Container Status ==="
            docker compose $COMPOSE_FILES ps frontend
            echo -e "\n=== Recent Logs ==="
            docker compose $COMPOSE_FILES logs --tail=30 frontend
            echo -e "\n=== Health Check ==="
            docker compose $COMPOSE_FILES exec -T frontend wget -q -O- http://localhost:80 | head -1 || echo "Health check failed"
